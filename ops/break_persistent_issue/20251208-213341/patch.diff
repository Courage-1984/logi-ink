diff --git a/css/components/navigation.css b/css/components/navigation.css
index 73d47c8..d525d35 100644
--- a/css/components/navigation.css
+++ b/css/components/navigation.css
@@ -6,7 +6,7 @@
   width: 100%;
   background: rgba(10, 10, 10, 0.9);
   backdrop-filter: blur(10px);
-  z-index: 1000;
+  z-index: 10000; /* High z-index to ensure navbar is above hero sections */
   padding: var(--space-4) var(--space-8);
   border-bottom: 1px solid rgba(0, 255, 255, 0.1);
   transition: all 0.3s ease;
@@ -151,6 +151,7 @@
 /* Dropdown Menu Styles */
 .nav-item-dropdown {
   position: relative;
+  z-index: 10001; /* Ensure dropdown parent is also above hero sections */
 }
 
 .nav-item-dropdown > .nav-link {
@@ -214,18 +215,20 @@
   min-width: 200px;
   opacity: 0;
   visibility: hidden;
-  transform: translateY(-10px);
-  transition: all 0.3s ease;
+  /* Removed transform from base state to prevent stacking context isolation */
+  transition: opacity 0.3s ease, visibility 0.3s ease, transform 0.3s ease;
   list-style: none;
   padding: var(--space-2) 0;
   box-shadow: 0 4px 20px rgba(0, 255, 255, 0.1);
+  z-index: 10001 !important; /* Higher than navbar z-index (10000) to ensure it appears above hero sections and all content - !important to override any conflicting rules */
+  /* Removed isolation: isolate - it was isolating the dropdown from navbar's stacking context */
 }
 
 /* Dropdown menu only opens on hover, not when active */
 .nav-item-dropdown:hover .dropdown-menu {
   opacity: 1;
   visibility: visible;
-  transform: translateY(0);
+  transform: translateY(0); /* Apply transform only when visible for slide-down animation */
 }
 
 .dropdown-menu li {
